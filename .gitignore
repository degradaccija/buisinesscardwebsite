# root-level ignores (site/ has its own .gitignore)
.DS_Store
supabase/.env
design-inspiration/
.playwright-cli/
.playwright/

supabase/.temp/

# session exports contain live secrets — never commit
export/
ope
